# Объяснение поля `calculated_count`

## Что это такое?

`calculated_count` — это **количество LogisticTrucks, у которых цена уже рассчитана** (т.е. `price != 0`).

## Как это работает?

### В вашем примере:

**Всего LogisticTrucks:** 8 записей

**С рассчитанной ценой (price > 0):** 5 записей
- ID 13: `price: 871.63` ✅
- ID 7: `price: 788.82` ✅
- ID 12: `price: 659.97` ✅
- ID 8: `price: 332.05` ✅
- ID 19: `price: 809.03` ✅

**Без рассчитанной цены (price = 0):** 3 записи
- ID 11: `price: 0` ❌
- ID 10: `price: 0` ❌
- ID 9: `price: 0` ❌

**Результат:** `calculated_count: 5` ← это количество записей с `price > 0`

## Где найти строки с ценой?

Смотрите в массиве `logistic_trucks`:

```json
{
  "logistic_trucks": [
    {
      "id": 13,
      "price": 871.63  ← ВОТ ОНА! Цена рассчитана
    },
    {
      "id": 7,
      "price": 788.82  ← И ЭТА ТОЖЕ!
    },
    // ... и так далее
    {
      "id": 11,
      "price": 0  ← ЭТА ЕЩЕ НЕ РАССЧИТАНА
    }
  ],
  "calculated_count": 5  ← Количество записей с price > 0
}
```

## Как это считается?

В коде (handler/logistic.go):

```go
calculatedCount := 0
for _, lt := range logistic.LogisticTrucks {
    if lt.Price != 0 {  // Если цена не равна нулю
        calculatedCount++  // Увеличиваем счетчик
    }
}
response.CalculatedCount = &calculatedCount
```

## Почему у вас 3 записи с price = 0?

Возможные причины:
1. **Они были добавлены после завершения заявки** — расчет запускается только при завершении
2. **Расчет еще не завершен** — нужно подождать еще 5-10 секунд
3. **Ошибка при расчете** — проверьте логи Django сервиса

## Как проверить в Postman?

1. **Найдите все записи с `price > 0`:**
   - В ответе ищите `"price":` в массиве `logistic_trucks`
   - Подсчитайте, сколько из них не равны 0

2. **Сравните с `calculated_count`:**
   - Должно совпадать!

3. **Пример:**
   ```json
   "calculated_count": 5  ← Должно быть 5 записей с price > 0
   ```
   
   И действительно:
   - ID 13: price = 871.63 ✅
   - ID 7: price = 788.82 ✅
   - ID 12: price = 659.97 ✅
   - ID 8: price = 332.05 ✅
   - ID 19: price = 809.03 ✅
   
   **Итого: 5 записей** ✅

## Визуально в вашем ответе:

```
✅ ID 13: price: 871.63  ← Рассчитано
✅ ID 7:  price: 788.82  ← Рассчитано
✅ ID 12: price: 659.97  ← Рассчитано
✅ ID 8:  price: 332.05  ← Рассчитано
✅ ID 19: price: 809.03  ← Рассчитано

❌ ID 11: price: 0       ← НЕ рассчитано
❌ ID 10: price: 0       ← НЕ рассчитано
❌ ID 9:  price: 0       ← НЕ рассчитано

calculated_count: 5  ← Правильно! (5 из 8)
```

## Что делать, если нужно рассчитать оставшиеся?

Если заявка уже завершена, но некоторые цены = 0:

1. **Прямой вызов Django сервиса для каждого:**
   ```
   POST http://localhost:8001/
   {
     "pk": 11
   }
   ```

2. **Или обновить вручную:**
   ```
   PUT http://localhost:8080/api/logistic-trucks/11/price
   {
     "price": 500.0,
     "token": "secret123"
   }
   ```

